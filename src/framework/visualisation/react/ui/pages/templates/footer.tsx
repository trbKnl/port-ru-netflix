interface FooterProps {
  left?: JSX.Element
  middle?: JSX.Element
  right?: JSX.Element
}

export const Footer = ({ left, middle, right }: FooterProps): JSX.Element => {
  return (
    <>
    </>
  )
}
